# Century cohort pack — mtc_aat_cohort.
#
# Sits on top of packs/core.yaml. Declares the actual warehouse schema to
# introspect and the per-table curation recipes that collapse raw OMOP
# columns into business variables.

cohort_name: mtc_aat_cohort
schema_name: mtc__aat_cohort


# Optional overlay for validate_dictionary.py (--cohort mtc_aat). Keys
# omitted here fall back to the validator's built-in defaults. Lists in
# the overlay are *appended* to the defaults; scalars replace them.
validator:
  recommended_schema_values:
    - infusion   # mtc-specific table for Administration notes

# Per-table recipes. ``mode`` values:
#   per_concept          - one row per distinct ``group_by`` value
#                          (observations, measurements)
#   single_row_with_list - one row; distinct group values populate Values
#                          (condition_occurrence, visit_occurrence)
#   split_by_type        - one row per value of ``group_by`` using the
#                          per-value override in ``splits``
#                          (drug_exposure: Prescriptions vs Administrations)
#   keep_columns         - one row per named demographic column
#                          (person / location / payer_plan_period)
#   static               - emit the hard-coded rows verbatim
#                          (notes / documents that are Unstructured)
curation_rules:

  observation:
    category: Observation
    mode: per_concept
    group_by: observation_concept_name
    value_column: value_as_number
    criteria_template: "observation_concept_name = '{name}'"
    description_template: "Observation '{name}' captured at the office visit."
    extraction_type: Structured
    max_concepts: 30

  measurement:
    category: Labs
    mode: per_concept
    group_by: measurement_concept_name
    value_column: value_as_number
    criteria_template: "measurement_concept_name = '{name}'"
    description_template: "Laboratory measurement '{name}' recorded for the patient."
    extraction_type: Structured
    max_concepts: 30

  condition_occurrence:
    category: Diagnosis
    mode: single_row_with_list
    variable: Diagnosis
    description: Medical diagnoses and conditions recorded for the patient.
    group_by: condition_concept_name
    value_column: condition_concept_name
    extraction_type: Structured
    max_values: 50

  drug_exposure:
    category: Prescriptions
    mode: split_by_type
    group_by: drug_type_concept_name
    value_column: drug_concept_name
    criteria_template: "drug_type_concept_name = '{name}'"
    extraction_type: Structured
    splits:
      "EHR prescription":
        variable: Prescriptions
        category: Prescriptions
        description: Medications prescribed to the patient.
      "EHR administration record":
        variable: Administrations
        category: Administrations
        description: Administrations recorded for the patient.

  procedure_occurrence:
    category: Procedures
    mode: single_row_with_list
    variable: Procedures
    description: Procedures recorded for the patient.
    group_by: procedure_concept_name
    value_column: procedure_concept_name
    extraction_type: Structured
    max_values: 50

  visit_occurrence:
    category: Visits
    mode: single_row_with_list
    variable: Visit Type and Date
    description: The type of healthcare visit or encounter.
    group_by: visit_concept_name
    value_column: visit_concept_name
    extraction_type: Structured
    max_values: 20

  person:
    category: Demographics
    mode: keep_columns
    columns:
      gender_concept_name:
        variable: Sex
        description: Biological sex at birth.
        variable_type: categorical
      race_concept_name:
        variable: Race
        description: Self-identified racial category.
        variable_type: categorical
      ethnicity_concept_name:
        variable: Ethnicity
        description: Self-identified ethnicity.
        variable_type: categorical
      year_of_birth:
        variable: BirthYear
        description: Year the patient was born.
        variable_type: continuous

  location:
    category: Demographics
    mode: keep_columns
    columns:
      zip:
        variable: ZIP Code
        description: Patient ZIP code (full or first 3 digits).
        variable_type: categorical
      state:
        variable: State
        description: Patient state of residence.
        variable_type: categorical

  payer_plan_period:
    category: Demographics
    mode: keep_columns
    columns:
      payer_concept_name:
        variable: Payer
        description: Insurance payer responsible for the visit.
        variable_type: categorical
      plan_type_concept_name:
        variable: Plan Type
        description: Insurance plan type.
        variable_type: categorical

  note:
    category: Documents
    mode: static
    rows:
      - variable: Clinical Note
        description: Free text of the office visit clinical note.
        column: note_text
        criteria: ""
        extraction_type: Unstructured

  document:
    category: Documents
    mode: static
    rows:
      - variable: Document
        description: Scanned PDFs (MRI, EKG, PET scan, infusion notes, etc.).
        column: document_type_concept_name
        criteria: ""
        extraction_type: Unstructured

  # NOTE: mtc__aat_cohort.infusion on this warehouse is drug_exposure-shaped
  # (dates, quantity, refills, days_supply, route_concept_name). It is not
  # a free-text/notes table here, so the "Infusion Note" static recipe was
  # removed. If a later ingest adds a note_text column, re-add the recipe.
  # Infusion *activity* is already covered by the drug_exposure
  # Administrations split above (drug_type_concept_name = 'EHR
  # administration record').
