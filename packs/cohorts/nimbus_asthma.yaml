# Cohort pack: Nimbus Asthma
#
# Nimbus curated asthma cohort (provider-level, excluding Nimbus AZ).
# Schema name matches the raw dump in Output/nimbusasthmacurated.pdf.

provider: Nimbus
disease: Asthma
display_name: Nimbus Asthma
cohort_name: nimbus_asthma_curated
schema_name: nimbus_asthma_curated
variant: raw
site_group: Nimbus
status: active

description: >
  Nimbus patients with an asthma diagnosis — intermittent,
  mild / moderate / severe persistent, and severity-unspecified
  variants. Curated view layered over the Nimbus base warehouse.

notes:
  - Technical dictionary generated from schema introspection.
  - Variables pack is `nimbus_asthma` (per-cohort). Currently a
    structural placeholder that inherits everything from the shared
    `asthma_common` / `respiratory_common` chain; cohort-specific
    rows land here if the Nimbus asthma curated ETL ships abstracted
    tables (e.g. IgE / ACT standardised).
  - FEV1 / FVC / FEV1-FVC ratio, ACT, and FeNO rows are abstracted;
    expect Implemented=No until site-specific abstraction mappings
    are confirmed.
  - Biologic row aggregates any-biologic exposure; per-ingredient
    attribution requires tightening drug_concept_name on the specific
    agent before reporting utilisation.

category_rules: {}

# Variables pack (Page 4) — per-cohort final pack.
variables_pack: nimbus_asthma
