# Cohort pack: Nimbus AZ COPD
#
# Nimbus Arizona-site COPD cohort. Schema name matches the raw dump
# in Output/nimbusazcopd.pdf.

provider: Nimbus AZ
disease: COPD
display_name: Nimbus AZ COPD
cohort_name: nimbus_az_copd_cohort
schema_name: nimbus_az_copd_cohort
variant: raw
site_group: Nimbus_AZ
status: active

description: >
  Nimbus Arizona-site patients with a chronic obstructive pulmonary
  disease diagnosis. Kept provider-separate from the broader Nimbus
  COPD cohort per the §1.1 site-consolidation decision.

notes:
  - Technical dictionary generated from schema introspection.
  - Variables pack is `nimbus_az_copd` (per-cohort). Currently a
    structural placeholder that inherits everything from the shared
    `copd_common` / `respiratory_common` chain; cohort-specific rows
    land here once the Nimbus AZ ETL starts shipping abstracted
    tables. Nimbus COPD's `eosinophil_standardized` row is NOT
    inherited because the table does not exist in this schema.
  - Per README §2.1, FEV1 abstraction is in progress for this site —
    the FEV1 / FVC / FEV1-FVC ratio rows are marked Abstracted and
    will remain Implemented=No until that pipeline ships.

category_rules: {}

# Variables pack (Page 4) — per-cohort final pack.
variables_pack: nimbus_az_copd
